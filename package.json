{
  "name": "skydata-backend",
  "version": "1.0.0",
  "description": "Backend API REST para SKYDATA Bogotá",
  "main": "src/presentation/server.js",
  "scripts": {
    "start": "node src/presentation/server.js",
    "dev": "node src/presentation/server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "jest tests/integration/",
    "test:acceptance": "jest tests/acceptance/",
    "test:security": "jest tests/security/",
    "test:performance": "jest tests/performance/",
    "test:compatibility": "jest tests/compatibility/",
    "test:reliability": "jest tests/reliability/",
    "test:load": "artillery run tests-load-stress-backend/load-test.yml --output test-results/load-report.json",
    "test:stress": "artillery run tests-load-stress-backend/stress-test.yml --output test-results/stress-report.json",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "security:audit": "npm audit",
    "quality:check": "npm run lint && npm run security:audit && npm run test:coverage"
  },
  "keywords": [
    "skydata",
    "bogota",
    "api",
    "rest",
    "geojson"
  ],
  "author": "SKYDATA-BOGOTA",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "artillery": "^2.0.0",
    "eslint": "^8.52.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
